<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>nl.vpro.poms</groupId>
    <artifactId>poms-parent</artifactId>
    <version>5.5.0</version>
  </parent>

  <groupId>nl.publiekeomroep</groupId>
  <artifactId>poms-shared-backend-parent</artifactId>
  <version>5.5-SNAPSHOT</version>
  <packaging>pom</packaging>
  <description>Shared backend implementation. The elasticsearch backend e.g. is used by both the frontend api as the publisher</description>

  <properties>
    <!-- Tied to ES version! -->
    <lucene.version>${lucene.elasticsearch.version}</lucene.version>
    <poms.shared.backend.version>${project.version}</poms.shared.backend.version>
    <poms.shared.version>5.6-SNAPSHOT</poms.shared.version>
    <skipIntegTests>true</skipIntegTests>
    <!--<integ.deps>${project.basedir}/../target/integration-tests/binaries</integ.deps>
    <integ.scratch>${project.basedir}/../target/integration-scratch</integ.scratch>
    <integ.http.port>9211</integ.http.port>
    <integ.java.port>9311</integ.java.port>
    <integ.temp>${project.basedir}/../target/integration-tests/tmp</integ.temp>
    <integ.cluster.name>api-test</integ.cluster.name>-->
    <pages.version>5.5-SNAPSHOT</pages.version>
    <vpro.shared.version>1.75-SNAPSHOT</vpro.shared.version>
    <assembly.tarLongFileMode>posix</assembly.tarLongFileMode>
  </properties>

  <scm>
    <connection>scm:svn:https://subversion.vpro.nl/repo/nl/publiekeomroep/poms-shared-backend/trunk</connection>
    <developerConnection>scm:svn:https://subversion.vpro.nl/repo/nl/publiekeomroep/poms-shared-backend/trunk</developerConnection>
    <url>https://subversion.vpro.nl/viewvc/nl/publiekomroep/poms-shared-backend/trunk</url>
  </scm>

  <modules>
    <module>api-backend-elasticsearch</module>
    <module>api-backend-media-search</module>
    <module>api-backend-thesaurus</module>

    <module>media-es-mappings</module>
    <module>pages-es-mappings</module>
  </modules>

  <dependencies>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>nl.vpro.shared</groupId>
      <artifactId>vpro-shared-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <repositories>
    <repository>
      <id>vpro</id>
      <url>https://nexus.vpro.nl/content/groups/public</url>
      <releases>
        <enabled>true</enabled>
      </releases>
      <snapshots>
        <enabled>true</enabled>
      </snapshots>
    </repository>
  </repositories>
  <pluginRepositories>
    <pluginRepository>
      <id>vpro</id>
      <url>https://nexus.vpro.nl/content/groups/public</url>
      <releases>
        <enabled>true</enabled>
      </releases>
      <snapshots>
        <enabled>true</enabled>
      </snapshots>
    </pluginRepository>
  </pluginRepositories>
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>nl.vpro.api</groupId>
        <artifactId>api-backend-elasticsearch</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>nl.vpro.media</groupId>
        <artifactId>media-es-mappings</artifactId>
        <version>${project.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>
  <build>
    <plugins>
      <!-- we doen het niet, en installeren een ES op jenkins -->
   <!-- &lt;!&ndash; http://david.pilato.fr/blog/2016/10/18/elasticsearch-real-integration-tests-updated-for-ga/ &ndash;&gt;

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>3.0.1</version>
        <executions>
          <execution>
            <id>integ-setup-dependencies</id>
            <phase>pre-integration-test</phase>
            <goals>
              <goal>copy</goal>
            </goals>
            <configuration>
              <skip>${skipIntegTests}</skip>
              <artifactItems>
                <artifactItem>
                  <groupId>org.elasticsearch.distribution.zip</groupId>
                  <artifactId>elasticsearch</artifactId>
                  <version>${elasticsearch.version}</version>
                  <type>zip</type>
                </artifactItem>
              </artifactItems>
              <useBaseVersion>true</useBaseVersion>
              <outputDirectory>${integ.deps}</outputDirectory>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <version>1.8</version>
        <executions>
          &lt;!&ndash; start up external cluster &ndash;&gt;
          <execution>
            <id>integ-setup</id>
            <phase>pre-integration-test</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <skip>${skipIntegTests}</skip>
              <target>

                <ant antfile="${project.basedir}/../src/test/ant/integration-tests.xml"
                     target="start-external-cluster"/>
              </target>
            </configuration>
          </execution>
          &lt;!&ndash; shut down external cluster &ndash;&gt;
          <execution>
            <id>integ-teardown</id>
            <phase>post-integration-test</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <skip>${skipIntegTests}</skip>
              <target>
                <ant antfile="${project.basedir}/../src/test/ant/integration-tests.xml"
                     target="stop-external-cluster"/>
              </target>
            </configuration>
          </execution>
        </executions>
      </plugin>-->
    </plugins>
  </build>
</project>
